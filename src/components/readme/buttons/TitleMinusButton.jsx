// src/layout_test/Buttons/TitlePlusButton.js
import React from 'react';

const TitleMinusButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>
      -
    </button>
  );
};

export default TitleMinusButton;
